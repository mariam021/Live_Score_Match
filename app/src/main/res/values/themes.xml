<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LiveScoreMatch" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="SearchBarShape" parent="Widget.AppCompat.Light.SearchView">
        <item name="android:background">@drawable/search_bar</item>
<!--        <item name="submitBackground">@color/primaryColor</item>-->
<!--        <item name="queryBackground">@color/primaryColor</item>-->
    </style>

    <style name="TextTheme">
        <item name="android:textSize">@dimen/text_normal</item>
        <item name="android:layout_weight">1</item>
    </style>

    <style name="TextTheme.Normal">
        <item name="fontFamily">@font/poppins</item>
        <item name="android:textColor">@color/light_black</item>
    </style>

    <style name="TextTheme.Bold">
        <item name="fontFamily">@font/poppins_bold</item>
        <item name="android:textColor">@color/dark_black</item>
    </style>

    <style name="Theme.LiveScoreMatch" parent="Base.Theme.LiveScoreMatch" />
</resources>